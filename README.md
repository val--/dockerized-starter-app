Dockerized Starter App (Symfony + React starter kit)
-------------

Work in progress ! 
<br>For now you can launch it with...


    gh repo clone val--/dockerized-starter-app && cd dockerized-starter-app
    composer install --working-dir=api
    make dev
